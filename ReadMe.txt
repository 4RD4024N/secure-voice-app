To start the project's signaling server, run the node server.js command in the project directory.

Then install ngrok on your computer and share the port with ngrok via the port to which localhost is connected.